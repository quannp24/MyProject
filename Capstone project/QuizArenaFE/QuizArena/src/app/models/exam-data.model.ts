export interface ExamData {
    examId?: string,
    examName?: string,
    quizId?: number,
    description ?: string,
    status?: number
    examType?: number
    date?: Date
    image?: string
  };
